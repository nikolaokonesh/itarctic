<% if partner.photo.attached? %>
  <div class="col-sm-4 p-2">
    <%= link_to image_tag(partner.photo, class: 'w-100'), partner.url, target: '_blank' %>
    <%= link_to 'Изменить', edit_partner_path(partner) if can? :manage, partner %>
  </div>
<% end %>
